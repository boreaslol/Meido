[package]
name = "vellum"
version = "0.1.0"
description = "Markdown reader with first-class Mermaid zoom"
authors = ["Vellum"]
edition = "2021"
rust-version = "1.77"

[lib]
name = "vellum_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
